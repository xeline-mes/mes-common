package com.xeline.core.util.io;

/**
 * 
 *
 * @author xenron
 * @since 1.0
 *
 */
public class CopyUtils {

  // public static Map<String, Object> asMap(Object o, CharSequence... propertyNames) {
  // return asMapExcludes(o, propertyNames);
  // }

  // @SuppressWarnings("unchecked")
  // public static Map<String, Object> asMapExcludes(Object o, CharSequence... propertyNames) {
  // Map<String, Object> params = Beans.createAndCopy(Map.class, o).excludesNull().excludes(propertyNames).excludesWhitespace().execute();
  // return params;
  // }

  // @SuppressWarnings("unchecked")
  // public static Map<String, Object> asMapIncludes(Object o, CharSequence... propertyNames) {
  // Map<String, Object> params = Beans.createAndCopy(Map.class, o).excludesNull().includes(propertyNames).excludesWhitespace().execute();
  // return params;
  // }

  // @SuppressWarnings("unchecked")
  // public static Map<String, Object> asMap(Object o) {
  // Map<String, Object> params = Beans.createAndCopy(Map.class, o).execute();
  // return params;
  // }

  // public static void copy(Object src, Object dest) {
  // Beans.copy(src, dest).execute();
  // }
  //
  // public static void copyExcludesNullAndWhitespace(Object src, Object dest) {
  // Beans.copy(src, dest).excludesNull().excludesWhitespace().execute();
  // }
  //
  // public static void copyExcludes(Object src, Object dest, CharSequence... propertyNames) {
  // Beans.copy(src, dest).excludes(propertyNames).execute();
  // }
  //
  // public static void copyExcludesNull(Object src, Object dest) {
  // Beans.copy(src, dest).excludesNull().execute();
  // }
  //
  // public static void copyExcludesWhitespace(Object src, Object dest) {
  // Beans.copy(src, dest).excludesWhitespace().execute();
  // }
  //
  // public static void copyIncludes(Object src, Object dest, CharSequence... propertyNames) {
  // Beans.copy(src, dest).includes(propertyNames).execute();
  // }
  //
  // public static <T> T createAndCopy(Class<T> destClass, Object src) {
  // return Beans.createAndCopy(destClass, src).execute();
  // }
  //
  // public static <T> T createAndCopyIncludes(Class<T> destClass, Object src, CharSequence... propertyNames) {
  // return Beans.createAndCopy(destClass, src).includes(propertyNames).execute();
  // }
  //
  // public static <T> T createAndCopyExcludes(Class<T> destClass, Object src, CharSequence... propertyNames) {
  // return Beans.createAndCopy(destClass, src).excludes(propertyNames).execute();
  // }
  //
  // public static <T> T createAndCopyExcludesNull(Class<T> destClass, Object src) {
  // return Beans.createAndCopy(destClass, src).excludesNull().execute();
  // }
  //
  // public static <T> T createAndCopyExcludesNullAndWhitespace(Class<T> destClass, Object src) {
  // return Beans.createAndCopy(destClass, src).excludesNull().excludesWhitespace().execute();
  // }

}
